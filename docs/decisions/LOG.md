# Decision log — every decision, one line each

> Newest on top. **Every** decision lands here, even the little ones — naming choices,
> library picks, a default flipped, a small tradeoff taken. Big or architectural decisions
> ALSO get a full ADR (link it from the line). The only thing that does NOT get logged:
> discussion that settled nothing — if it changed the code or answered a question, it
> belongs here.

Format: `YYYY-MM-DD — <what was decided> — <why, in a few words> (ADR-NNNN if any)`

> **Growth rule (CONVENTIONS §1):** when this log gets long, split by year —
> `LOG-YYYY.md` files, newest year on top of each, this file becomes the index
> (one line per year + where the current year lives), linked from `README.md`.

---

- 2026-08-13 — (b8.5) skin contract made LOOK-AWARE: corridor centres on the POOL'S measured skin hue (skin-family bins), not a canonical 38° — one canonical target SWAPPED the two looks' skin personalities (user verdict: do-revenge bright-pale-magenta vs shirley rich-orange rendered inverted); absolute walls stay look-agnostic (never past 15° magenta; chroma in [0.7×, 1.6×]) (ADR-0008)
- 2026-08-13 — (b8.5) mixed pool now learns BOTH profiles in one run ("name — bright" + "name — dark", saved as -bright/-dark): user's footage is mixed too; one-output-per-learn forced manual frame surgery (ADR-0008)
- 2026-08-13 — (b8.5) GUI targets-cache BUG fixed: precomputed pool targets bypassed the lighting grouping entirely — the GUI was learning mixed pools blended into neither look; cache removed, correctness over seconds of ingest (ADR-0008)
- 2026-08-13 — docs/ + tests/ now TRACKED in git (user call, reversing the init-time local-only policy): the D: drive failure made the single-point-of-failure cost concrete — project memory and the safety net belong in version control
- 2026-08-13 — (b8.5) skin corridor asymmetry widened warm-ward (2×→5× tol ≈ 17°): the 6.9° warm corridor vetoed legitimate warm film tints (Block S learned all-zero poles under it); film warms skin freely — only magenta stays hard-blocked at 3.4° (ADR-0008)
- 2026-08-13 — (b8.5) BLOCK S added — luminance-conditional tint curve (5 Oklab a/b poles aligned to poolstats' L bands): user verdict "adding contrast, not transforming color" — measured: refs paint a warm ARCH (b .005/.026/.032/.027/.019) that every existing block (gammas/crosstalk/lights = monotone drifts) is structurally unable to express (ADR-0008)
- 2026-08-13 — (b8.5) Block S evolved 2 poles → 3 → 5 measured against band targets (2 poles can't paint an arch; 3 poles misalign with the 5 measurement bands); fitted jointly with crosstalk (the balance-signal stage); colour dial scales it; max band gap 0.020→0.0127 (ADR-0008)
- 2026-08-13 — (b8.5) SKIN CONTRACT (user requirement: film emulation must ALWAYS protect skin): canonical skin probes ride through EVERY fit stage — magenta-ward hue drift hinge-penalized 2× tighter than warm-ward (~3.4°/6.9° corridors), skin chroma capped ×1.35; §6 gains a skin gate (8°/×1.6, looser: catches broken looks not style) (ADR-0008)
- 2026-08-13 — (b8.5) s0 (whole-wheel hue rotation) capped ±0.03 rad — a global rotation IS a cast, not film character (film twists locally); s0=5° was painting the user's whole-scope magenta drift (ADR-0008)
- 2026-08-13 — (b8.5) band_balance_weight 3→6, zone_weight 1.5→2.5 — tone-quantile residuals let the pool's pink PALETTE leak in as a +0.02a global cast (magenta skin); balance/angle residuals must be able to veto marginal channel pushes (ADR-0008)
- 2026-08-13 — (b8.5) validator skin probes must be converted to the CUBE'S INPUT domain (inverse-base) — display-space patches fed to a DI-input cube measure deep shadows, not skin (found via a phantom 47.8° violation while world stats were clean) (ADR-0008)
- 2026-08-13 — (b8.5) hue curve RE-FIT after polish (warm start, IDENTICAL residual set): polish re-tunes tone on top of frozen trims — red family double-boosted (user: "pink/red exaggerated"; measured 1.7–2.2× relative, trims +24%) (ADR-0008)
- 2026-08-13 — (b8.5) trim coefs bounded ±0.15/k (≈±30% worst-case per-hue — the physical film trim range); red overshoot 2.18×→1.17×, trims 24%→14% (ADR-0008)
- 2026-08-13 — (b8.5) §6 endpoint tol 0.16→0.17 — legit film fits measure white drift to 0.166; over-driven failure case (0.24) still caught (ADR-0008)
- 2026-08-13 — (b8.5) twist test asserts curve error AT THE HUE-MASS PEAK, not global argmax — the ridge-flattened far side makes argmax jump lobes (noise) (ADR-0008)
- 2026-08-13 — (b8.5) aligned-path exposure PINNED at 0 (bounds, not ridge): Hunt/spread residuals let the fit brighten the world +0.18 DI to fake punch, stripped on ship = the wash again; bake path keeps exposure free (ADR-0008)
- 2026-08-13 — (b8.5) tile-p95 debias REMOVED from the spread residual — real-frame max-over-tiles = most-colourful-patch tail; demanding it drove 2–4× oversaturation and every tone param to its rail; statistic stays diagnostic-only (ADR-0008)
- 2026-08-13 — (b8.5) printer lights bounds ±0.5→±0.3 stop (blue +0.38 painted magenta contamination), print slope cap 1.9→1.8, spread_weight 2→1.5 — first real-pool tames (ADR-0008)
- 2026-08-13 — merged b8.4 + pushed main (73b31ff..0dcff51) — first push since the D: drive death; all of 8.1–8.4 now offsite on GitHub (user approval)
- 2026-08-13 — (b8.4) §6 endpoint tolerance 0.10→0.16 — a film print's range is NARROWER than the conversion's BY DESIGN (D-max blacks, paper-white rolloff; Dehancer's Analog Range Limiter); measured preset drift black 0.10 / white 0.14; wild fits still caught (ADR-0008)
- 2026-08-13 — (b8.4) trim-coef ridge 4×→10× — Block F cannot null a level-shifted world exactly, and the trims chased the residue as phantom sat wiggle (tc1 hit 0.24 on the darkened-world guard) (ADR-0008)
- 2026-08-13 — (b8.4) darkened-world guard asserts RENDERED outcome per path (aligned: no dulling vs input; bake: vs reference — C/L is level-dependent so each path compares at its own level) + structural level protections (t0 zeroed, satluma neutral); parameter forensics dropped (ADR-0008)
- 2026-08-13 — (b8.4) chroma-marginal adoption into the aligned source world TRIED + REVERTED — made the phantom worse (trims 0.25) and broke the alignment contract; marginals cannot carry the joint per-hue structure anyway (ADR-0008)
- 2026-08-13 — (b8.4) hue-family polish factor 0.6→0.8 — Block F's physics bounds sculpt per-hue brightness less freely than legacy's 12 display-curve params; tradeoff bought filmic structure (ADR-0008)
- 2026-08-13 — (b8.4, in flight) PrinterLights added to Block F — per-channel print-stage exposure offsets: the physical cast mechanism; without them stage-2 crosstalk absorbed balance signal it can't express (gr slammed −0.15) (ADR-0008)
- 2026-08-13 — (b8.4) coupling fitted SYMMETRIC (3 pair params): its asymmetric part is a hue rotation invisible to chroma statistics — left free it floats on noise and corrupts the hue curve (ADR-0008)
- 2026-08-13 — (b8.4) coupling stays NEUTRAL until its own stage; preset coupling in stages 1-2 pre-rotates the world and mis-routes the balance signal (ADR-0008)
- 2026-08-13 — (b8.4) preset anchoring made CONFIDENCE-AWARE for knee strengths: anchor to film only where the pool's tail bands are thin; ridge_tone 0.05→0.03 (identity world kept a 6% phantom shoulder) (ADR-0008)
- 2026-08-13 — (b8.4) synthetic recovery tests ported to v3-CLASS truths (film system + personality): free recovery of non-film worlds is no longer the engine's claim (ADR-0008 core thesis); Block E asserts the rendered dark/bright split statistic, not l-coefs — Block F itself produces brightness-dependent hue rotation, making parameter recovery degenerate (ADR-0008)
- 2026-08-13 — soft-l1 loss REJECTED as fit default: scipy's robust-loss Jacobian reweighting crushes the small hue-angle residuals (hue-twist recovery corr 0.92→0.38 measured); median pooling already covers outlier robustness (ADR-0008)
- 2026-08-13 — saturation-spread (punch) residual lives in the POLISH stage only: in stage 1 it bent the tone landscape (not exposure-invariant en route; satluma dragged to 0.75 compensating), in stage 3 it drowned hue personality (corr 0.78→0.46) (ADR-0008)
- 2026-08-13 — sat TAIL statistics use luma floor 0.15 (not 0.05): darkened frames inflate near-black pixels into fake sat tails; fit then learned real shadow-desat + stolen exposure chasing them (ADR-0008)
- 2026-08-13 — tile debias = MAX over 4×4 tiles of per-tile sat p95 (mean dilutes worse than global p95 — a 1-tile subject drowned in 15 dull tiles); in-tile p95 keeps it robust to lone pixels (ADR-0008)
- 2026-08-13 — Hunt compensation applied per LUMA BAND, not globally: a global factor cancels exactly in the level-free shape normalization (ADR-0008)
- 2026-08-13 — tail_weight + hunt_alpha default OFF in the legacy stage layout: ablations show they perturb tone enough to break hue-block recovery, and the legacy model has no punch lever (print slope) to absorb them — b8.4 activates them in the Block-F layout (ADR-0008)
- 2026-08-12 — preset negative gammas from LAD print-through ratios (R 0.966/1.063=0.909, B 1.082/1.063=1.018), NOT raw Vision3 neg γ ratios — LAD is the measured composed neutral tracking; raw 0.48/0.57 double-counts what printer lights correct (ADR-0008)
- 2026-08-12 — Block F placed BETWEEN A (crosstalk) and B (legacy display curves) in the pipeline — new fits use F with B neutral, old profiles use B with F neutral, both paths bit-exact for their profiles (ADR-0008)
- 2026-08-12 — dial split extended to F by same decomposition rule: tone = mean neg γ + toe + whole print stage; color = per-channel γ deviation + DIR coupling (ADR-0008)
- 2026-08-12 — Block F works in STOPS (log2(lin/0.18)), not log10 — all thresholds (toe_at, range_hi/lo, knee softness) read directly in stops; caught as a units bug where knees never engaged (ADR-0008)
- 2026-08-12 — soft knees are softplus-based with grey re-anchoring (exact fixed point at 18% grey) — knee-low sign fixed: term must ADD for slope 1−a below the knee; the subtracting form EXPANDS shadows (test-caught) (ADR-0008)
- 2026-08-12 — coupling parameterized as ≥0 suppression amounts, applied with negative sign, rows sum 0 — neutral preservation and saturation-non-decrease hold BY CONSTRUCTION, not by bounds (ADR-0008)
- 2026-08-12 — Tone/Color dial split redefined by DECOMPOSITION: tone = exposure + mean curve shape, colour = per-channel curve deviation (+A/C/D) — the cast lives in channel differences, not in "the curves block"; block-level grouping put it in the wrong dial (user screenshot)
- 2026-08-12 — exposure_align default flipped back ON (user: output = "desaturated + dark + lift shadow, barely different"): fitting curves AROUND a learned exposure and then shipping WITHOUT it left them in the wrong domain — the wash. Coherent pairs: don't-bake-brightness ⇔ shape-only fit (default); bake-brightness ⇔ full-mood fit. Verified: render now brighter than base (0.447 vs 0.413), no lift, greens luminous (ADR-0007)
- 2026-08-12 — AUTOMATIC LIGHTING GROUPING (user: "does my app intelligently know day vs dark?"): pools spanning >0.18 median-luma split by 1-D 2-means, ±0.04 stragglers excluded, BRIGHTER group preferred when ≥3 frames (dim web frames are the less trustworthy half); exclusion surfaced in UI/CLI, never silent. Auto-selection reproduces the hand-picked day group exactly on the real pool (ADR-0007)
- 2026-08-12 — asymmetric saturation-level residual TRIALLED AND REVERTED — inverted-sign bug found, and even corrected it degraded the verified hue-luminance result (greens 1.145→0.98); level-free stands, sat gap (−17% vs reference) documented as the known limit pending the film-character prior (ADR-0007)
- 2026-08-12 — PER-HUE LUMINANCE learned (user: "your greens don't match"): hue_mean_l targets (soft-binned, level-free vs mean L) + a 4th POLISH stage re-fitting tone+crosstalk with the hue field frozen — stage 3's blocks preserve L by construction, and putting the residual in stages 1-2 let curves steal hue-rotation signal (measured: twist corr 0.83→−0.10). Greens: 0.84→1.15 vs ref 1.21 (ADR-0007)
- 2026-08-12 — ONE LOOK = ONE LIGHTING CONTEXT (proven on the user's pool: 17 frames spanning median luma 0.04–0.46 blended night-warm-dark + day-pastel into a look matching neither; the 6 day frames alone produced exposure −0.3, neutral curves, correct hues, clean render). Product guidance in the Learn tab; automated brightness-spread warning = backlog (ADR-0007)
- 2026-08-12 — FILM BRIGHTNESS IS OPT-IN (user's 3-way comparison: bake darker than both their original AND the bright reference frame): pooled exposure is dragged by night scenes = content; default keeps the footage's exposure, "Bake film brightness" checkbox / --film-brightness flag opts in; the recipe still stores the fitted value (ADR-0007)
- 2026-08-12 — THE VIVIDNESS CONTRACT (user: "my colors turn brown, film is pop"): saturation LEVEL is never learned — web-still statistics under-measure a film's perceived vividness (compression + dark grades), so level-chasing dulls the user's footage. Learned: hue personality, tone, and RELATIVE saturation shape only (satluma mean-free within [0.7,1.3]; hue-trim DC zeroed; band + hue-magnitude residuals mean-centered). Trade documented: some ground-truth stat-matching accuracy given up deliberately (ADR-0007)
- 2026-08-12 — neutral prior = CONSTANT-SATURATION world (C/L 0.13 flat; chroma = 0.13·L) — the flat-chroma prior implied sat 0.30 in shadows, a phantom the fit chased with −35% cuts (ADR-0007)
- 2026-08-12 — Block E = brightness-LINEAR hue modulation on the Fourier basis (F1 order-2, 5 params) — the spec's hue×luma grid, expressed smoothly; most-regularized block (ridge 0.5×k², bounds ±0.2/k) (ADR-0007 b7.2)
- 2026-08-12 — internal cloud re-adopts the pool's hue structure (2nd attempt, now WITHOUT the mass residual that fought it) — hue-curve constants are invisible in marginal stats under a uniform wheel (b7.2)
- 2026-08-12 — Block E honest contract: direction + ~1/3 magnitude on synthetic worst case, no sign flips; asserted as such, not oversold (b7.2)
- 2026-08-12 — USER: curve visualization rejected; film-character prior PARKED as backlog note — continue ADR-0007 b7.2 (Block E)
- 2026-08-12 — colourfulness targets are SATURATION (chroma/L), not raw chroma — chroma scales with brightness, so dim pools taught desaturation on top of darkening; brightness-invariant targets encode "dark but vivid" (ADR-0007; user: "dull, baked over")
- 2026-08-12 — fine hue statistics SOFT-binned (triangular over 2 nearest centres) — hard binning quantizes the statistics, the finite-difference Jacobian goes inconsistent and trf's trust region collapses; stage 3 was literally unoptimizable (ADR-0007)
- 2026-08-12 — Fourier coef bounds decay as base/k + ridge grows as k² — bounds the hue curve's DERIVATIVE (steep curves compress hues into ΔE spikes); the classic curvature smoothness prior (ADR-0007)
- 2026-08-12 — fit source hue distribution = UNIFORM; mass residual + ref-structure adoption tried and rejected (structure adoption makes the mass term fight the true shift) — identifiability truth: a global rotation of a uniform wheel is invisible in marginal statistics; localized hue structure is what's learnable, and is what film looks are (ADR-0007)
- 2026-08-12 — hue residuals compared in ANGLE + CHROMA-RATIO units, not raw a/b — raw differences scale with chroma (~0.05) and drown under the ridge (ADR-0007)
- 2026-08-12 — finite-difference secant widened to 0.02 — sample-cloud statistics carry O(1/n) quantization; a wide secant averages over it (ADR-0007)
- 2026-08-12 — satluma bounds tightened to [0.4, 1.7] — a fitted 1.9× mid-chroma multiplier produced channel reversals on saturated ramps; film-plausible range only (ADR-0007)
- 2026-08-12 — crosstalk stage no longer sees hue residuals — pre-Fourier it absorbed hue twists; hue detail belongs to the Fourier stage (ADR-0007)
- 2026-08-12 — validator monotonicity: per-channel @5% only (legit looks ≤3% incl. hue-curve wiggle + base corner behaviour; broken 19–73%); display-luma variant tried + rejected (base's own saturation compression dips ~2% under legit mixing) (ADR-0007)
- 2026-08-12 — draft (--fast) fits get stiff colour ridge 0.25 — small clouds make wiggly hue curves; drafts trade fidelity for speed (ADR-0007)
- 2026-08-12 — CLI plumbing test applies with --force (a 5-frame noise-pool draft is LEGITIMATELY gate-flagged); v1-smoothness acceptance rewritten: v2.1 spends node-smoothness on hue fidelity but must stay inside the gate's budget (ADR-0007)
- 2026-08-12 — colour targets made CONDITIONAL: mean Oklab a/b per luminance band (band_mean_ab), median-pooled, conf-weighted in every fit stage — day-warm/night-cool no longer average into one cast; a LUT conditions on pixel brightness, the honest approximation of scene intent (ADR-0006 A)
- 2026-08-12 — exposure gets its own ridge 0.4 (vs 0.05 curves) — the lazy global-darkening fit must lose to conditional explanations; pure-exposure looks still recovered (ADR-0006 C)
- 2026-08-12 — user's corrupt Resolve still (064946_1.1.1.png, non-PNG bytes) detected — first-file corruption, not code; flagged for re-export
- 2026-08-12 — look intensity split into TONE amount (G+B) and COLOR amount (A+C+D) dials — content-leaked tonal mood is relaxable per shot while keeping the palette; convex interpolation toward neutral preserves every fit guarantee; amounts clamp to [0,1], no overdrive (ADR-0005)
- 2026-08-12 — amount dials are per-use bake modifiers: export uses the scaled model, save-as saves the editor's model unscaled (ADR-0005)
- 2026-08-12 — USER DECISION: Learn = reference stills only, apply later on any footage — the original product agreement; source-pool learning stays API-only, no GUI (ADR-0004 4.2)
- 2026-08-12 — Block G global exposure = DI code offset applied FIRST (log space: offset = exposure), bounds ±0.3 ≈ ±4 stops — S-curves anchored at (0,0)/(1,1) cannot express level; this was the ADR-0003 bound-slam root cause (ADR-0004 4.1)
- 2026-08-12 — exposure_align default OFF — with Block G the level is expressible, so the full look incl. tonal mood is learned vs the normal-world prior; align stays as a future colour-science-only knob (ADR-0004 4.2)
- 2026-08-12 — multi-still preview: per-still endpoint cache keyed by index; look/placement changes clear it; a bake finishing for a scrubbed-away still is cached but not rendered (ADR-0004 4.3)
- 2026-08-12 — L-002 mitigation confirmed: OPENBLAS_NUM_THREADS=1 for full-suite runs
- 2026-08-12 — letterbox auto-crop ON by default at ingest (edge-run mean-luma < 0.02, 2 px inset, refuse >50% crop) — bars poison every tone statistic; dark scenes untouched (ADR-0003 R.1)
- 2026-08-12 — source world adopts the POOL'S luma distribution (neutral per channel) instead of the fixed prior ramp — absolute tone reshape is unknowable from screenshots (spec hard truth); learnable = per-channel deviation + colour blocks at matched exposure; naive median scaling rejected (kills highlights, slopes hit lower bound) (ADR-0003 R.2)
- 2026-08-12 — ground-truth fitter tests run exposure_align=False — they test raw mechanics with a KNOWN source world; production keeps alignment ON (R.2)
- 2026-08-12 — acceptance-test frames made spatially smooth (4×4 bilinear-upscaled) — per-pixel uniform noise has a chroma distribution no camera produces; it pushed corner wiggle past the reversal threshold (R.2)
- 2026-08-12 — proxy-first preview NOT implemented — measured full-quality refresh 225 ms (bake 165 + still 59), under the 300 ms spec target; proxy would add complexity for nothing (b2.5)
- 2026-08-12 — pooled-stats caching via a pool_targets() seam in orchestration/learn.py + cache in LearnTab keyed by the path tuple — spec §9 "a dial tweak must never re-run Learn statistics" (b2.5)
- 2026-08-12 — DPI 100/125/150% visual pass left as MANUAL user QA — offscreen tests cannot see actual rendering; layout rules (scroll areas, splitter, min size, ellipsis) are the automated part (b2.5)
- 2026-08-12 — legacy tab named "Match (legacy)" per spec §9 mode naming; last-used mode persisted in QSettings (b2.4)
- 2026-08-12 — recipe editor widgets generated from one spec table over serialize.py dict paths — model↔UI stays mechanical; adding Block E in v2.1 = adding spec rows (b2.3)
- 2026-08-12 — editor spinbox ranges = the fit bounds — the same "no degenerate result" guarantee for hand edits as for fitted values (b2.3)
- 2026-08-12 — editor display precision 4 decimals — set_model→model() exact to 1e-4; beyond that is display rounding, documented in the brief (b2.3)
- 2026-08-12 — Apply preview: strength = cached-endpoint image lerp (exact for "between", close approx under node2 clamp); EXPORT always bakes the exact cube at the chosen strength — preview speed never compromises the artifact (b2.2)
- 2026-08-12 — profile recents persisted in QSettings("ReinaLook") max 8; tests isolate via ini-format path redirect (b2.2)
- 2026-08-12 — GUI export gate mirrors the CLI gate; dialog behind a `_confirm_force` seam so tests stub it without modal exec (b2.2)
- 2026-08-12 — Phase 2 planned as ADR-0002 (Learn/Apply UI, 5 batches) — spec §9/§10 Phase 2; never-block-UI + no-math-in-app rules carried as batch constraints
- 2026-08-12 — fit Cancel = cooperative flag checked at stage boundaries — seconds-long stages make finer granularity unnecessary; no thread killing (ADR-0002 b2.1)
- 2026-08-12 — recipe display units: hue shifts in degrees, trims/multipliers in percent, near-neutral values omitted — colorists read degrees/percent, not radians/raw floats (b2.1)
- 2026-08-12 — Learn tab ships in 2.1 beside legacy tab; full mode restructure deferred to 2.4 — usable increment per batch (b2.1)
- 2026-08-12 — PySide6 installed in dev env — GUI tests must actually run, not skip (b2.1)
- 2026-08-11 — Blocks C/D moved to CODE-SPACE Oklab (Oklab on DI code values, not scene-referred via DI decode) — scene Oklab explodes at lattice corners (DI 1.0 = linear ~100 → L≫1) causing ΔE 0.45 spikes + reversals, found by the 1.7 harness; code space is bounded and DI log ≈ perceptual; fit absorbs the difference (batch 1.7)
- 2026-08-11 — validation thresholds RELATIVE to the placement's reference cube (base/identity) — the base's own curvature is not a violation (batch 1.7)
- 2026-08-11 — tone-reversal metric = fraction of ramp steps with slope < −0.25; violation above 1% — measured: fits ≤0.55% (gamut-corner wiggle), broken blocks ≥3% (systemic); worst-case single step can't separate the regimes (batch 1.7)
- 2026-08-11 — export gate in CLI apply (not inside the pure bake) — bake stays reusable; gate prints per-block blame, refuses write, exit 3, --force override (batch 1.7)
- 2026-08-11 — v2 bake: "between" = direct model forward (model is natively DI→DI, no inverse round-trip needed — simpler + exacter than v1's OT path); "node2" = base composed AFTER the model (batch 1.6)
- 2026-08-11 — CLI `learn --fast` = FitOptions(n_samples=1200, max_nfev=30) — draft-quality fit for iteration; production default stays full (batch 1.6)
- 2026-08-11 — frame-count guidance printed on every learn run (1 frame = explicit scene-contamination warning) — honest-limits requirement from spec §4/§7 (batch 1.6)
- 2026-08-11 — Look Profile schema: strict on envelope (format/version), lenient on params (missing→neutral, unknown→ignored) — hand-editing is a feature; recipe must survive trims and future fields (batch 1.5)
- 2026-08-11 — params serialization lives pure in filmmodel/serialize.py, file IO in orchestration/profile.py — Phase-2 recipe editor reuses the dict layer without touching files (batch 1.5)
- 2026-08-11 — fit loss = statistics of a synthesized source cloud pushed through (base ∘ model ∘ inverse-base) vs pooled reference targets — the spec §5 method, measured with the SAME poolstats definitions both sides (batch 1.4)
- 2026-08-11 — stage bounds: toe/shoulder [0,2], slope [0.5,2], pivot [0.3,0.7], crosstalk ±0.25, sat [0.2,2], shift ±0.35 rad, trim ±0.5 — guarantee monotonic curves (tangent < 3 Fritsch limit) + diagonal-dominant matrix; no fitted result can be degenerate (batch 1.4)
- 2026-08-11 — per-region regularization = conf-damped residuals (sqrt(w/(w+w0)), w0=0.02) + per-stage ridge to neutral — thin bins relax to identity instead of fitting noise; proven by zero-weight-zone test (batch 1.4)
- 2026-08-11 — fit.py NOT exported from fitter/__init__ — poolstats imports fitter.filmmodel, fit imports poolstats; keeping fit out of the package namespace avoids the cycle (batch 1.4)
- 2026-08-11 — poolstats pooling = per-bin MEDIAN for values, MEAN for weights — median rejects outlier frames (proven in test: mean moves 5× more); weights must stay proportional for thin-data regularization (batch 1.3)
- 2026-08-11 — hue-zone stats exclude pixels with chroma < 0.01 — achromatic hue angle is numerical noise; zone binning = nearest ZONE_ANGLES centre, shared with Block D (batch 1.3)
- 2026-08-11 — neutral-prior values are documented heuristics (gray L 0.45, chroma peak 0.06 mids, black 0.02/white 0.95) — weak regularization only, never the core method per spec §4 (batch 1.3)
- 2026-08-11 — v1 lint debt (9 ruff findings) NOT fixed inside 1.3 — scope discipline; logged L-001, gather on fixes/2026-08 (batch 1.3)
- 2026-08-11 — DI↔Oklab bridge lives in engine/perceptual.py, NO clipping — that file's one concept is Oklab conversion; log working space must round-trip lossless for the sacred base (batch 1.2)
- 2026-08-11 — Block C curve = smoothstep-eased segments with zero-slope anchors — C¹ without a spline dependency; flat extrapolation free (batch 1.2)
- 2026-08-11 — Block D zone centres = Oklab hues of sRGB primaries/secondaries; C¹ periodic smoothstep interpolation with plateaus at centres — no hue breaks at zone boundaries (spec §6) (batch 1.2)
- 2026-08-11 — gray-axis chroma test tolerance = 1e-6 — Bradford DWG→709 matrix leaves ~1e-7 residual chroma in float64; numerical floor, invisible (batch 1.2)
- 2026-08-11 — Block B curve family = monotone cubic Hermite through diagonal anchors (0,0)/(pivot,pivot)/(1,1) — guarantees C¹ + strict monotonicity + exact identity@0 + fixed black/white; resolves ADR-0001 §1.1 curve-family risk (batch 1.1)
- 2026-08-11 — Block A crosstalk matrix = dest-row, applied `rgb @ M.T`, rows sum to 1 — preserves both energy-per-output and the neutral axis (row-stochastic `@ M` cast greys); matches engine/spaces.py convention (batch 1.1)
- 2026-08-11 — Phase 1 = v2 parametric film-emulation fitter core, no UI — the actual product goal (learn a film-shaped color science from references), sequenced as 7 batches (ADR-0001)
- 2026-08-11 — parametric fitting (~40–80 params, hard ceiling 80) replaces free-form OT/MKL as primary engine; v1 OT kept as "Match (legacy)" — data not model size is the bottleneck; parametric = emulation not memorization (ADR-0001, spec §11)
- 2026-08-11 — staged fitting + per-region regularization declared non-negotiable — what makes ~70 params stable and thin-data regions safe (ADR-0001, spec §5)
- 2026-08-11 — deterministic scipy least-squares, no AI — preserves explainable-math + offline positioning (spec §11.5)
- 2026-08-11 — moved REINALOOK_V2_FILM_EMULATION_SPEC.md to docs/ — vision doc is local-only per "git tracks code only" policy
- 2026-08-11 — Golden Rule = "engine/ + fitter/ are pure math (no I/O, no network, no AI); base conversion sacred (strength 0 = base bit-for-bit)" — collapses architecture decisions into one line
- 2026-08-11 — brief mode = in-file — repo already carries @context/@done/@todo/@limits/@affects docstrings in every source file
- 2026-08-11 — dev mode = serial; enforcement hooks = ON; tests/ = local-only (gitignored) — chosen at init
- 2026-08-11 — kept the 26 already-tracked test files tracked (no `git rm --cached`); `/tests/` gitignore applies to NEW test files only — avoids stripping tests from the public GitHub repo (user call)
